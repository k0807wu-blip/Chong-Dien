services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: chongdien
      POSTGRES_PASSWORD: chongdien_dev
      POSTGRES_DB: chongdien
    ports:
      - '5433:5432'
    volumes:
      - chongdien_pgdata:/var/lib/postgresql/data

volumes:
  chongdien_pgdata:
